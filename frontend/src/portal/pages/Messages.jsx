import PlaceholderPage from '../../shared/PlaceholderPage';
export default function Messages() {
  return (
    <PlaceholderPage
      title="Messages"
      subtitle="Not in PRD/Rules scope — stretch goal if line-level comments (B8) get promoted to full threads."
      phase="Out of scope for MVP"
    />
  );
}
